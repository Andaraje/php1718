{% extends 'base.html.twig' %}

{% block body %}
    {{ parent() }}
<div class="container">
    <h2> <a href="{{ path('perfil') }}">Atrás</a></h2>
    <h1>Editar un Hilo</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
    {{ form_end(edit_form) }}

</div>
    {% endblock %}
